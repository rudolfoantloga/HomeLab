services:
  uptime-kuma:
    image: louislam/uptime-kuma:1
    container_name: uptime-kuma
    restart: unless-stopped

    ports:
      - "3001:3001"

    volumes:
      - D:\HomeLab\volumes\uptime-kuma:/app/data

    networks:
      - uptime-kuma

networks:
  uptime-kuma:
    driver: bridge